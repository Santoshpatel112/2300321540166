# Notification App Backend

A service for managing and sending notifications to users.

## Features
- Send notifications (Email, SMS, Push).
- Retrieve notification history for a specific user.
- Integrated centralized logging.

## Endpoints
- `POST /send`: Send a notification.
- `GET /history/:userId`: Get history for a user.
